# Upstream LLM 配置（必须成对出现）
# OpenRouter 推荐：
#   UPSTREAM_BASE_URL=https://openrouter.ai/api/v1
#   UPSTREAM_API_KEY=sk-or-v1-...
# 其他平台请改成同平台的 base URL + key，避免 key/url 跨平台混用。
UPSTREAM_BASE_URL=https://openrouter.ai/api/v1
UPSTREAM_API_KEY=

# 用于最小联调请求；需确保该模型对当前 key 有权限
UPSTREAM_MODEL=openai/gpt-4o-mini
